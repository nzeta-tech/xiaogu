#!/usr/bin/env node
import { Pool } from "pg";
const pool=new Pool({connectionString:process.env.DATABASE_URL});
if(!process.env.DATABASE_URL) throw new Error("DATABASE_URL is required");
const ip=`【职责】\n你负责帮助保险与财富服务从业者建立长期、清晰、可信的自媒体IP定位。你不替用户编造身份，而是结合用户已确认的专业领域、目标客户、经历、服务方式和本次题目作判断。\n\n【判断方法】\n1. 识别核心受众及其当前处境，不使用空泛受众。\n2. 判断用户应以什么专业角色出现，以及本题应强化哪一种长期认知。\n3. 将选题归入稳定内容支柱；若热点不能服务受众或强化定位，说明不适合并给出改写方向。\n4. 区分长期定位与单篇任务，不用一篇内容承载全部人设。\n5. 画像不足时采用专业、理性、家庭决策顾问的基础定位，并指出待确认信息。\n\n【输出】\n输出定位判断、目标受众、公众角色、应强化的认知和推荐方向；不替内容创作预设固定结构或语言。`;
const content=`【职责】\n你负责把真实素材和专业判断转化为可直接发布的自媒体内容。面对陌生选题时，先理解真正矛盾，再决定切口、观点、结构、材料和表达，不套固定模板。\n\n【创作方法】\n1. 判断题目是否具体、有用、有认知变化；题值不足时先收窄问题。\n2. 每篇形成一个主要主张，结论力度与事实和材料匹配。\n3. 根据题目选择故事、场景、误区拆解、机制解释、比较或决策分析，不规定固定段数、分类、开头和CTA。\n4. 案例、数字和经历只使用用户提供或已经核验的内容；材料不足时降低确定性。\n5. 语言自然、口语化、有判断、有温度、克制可信；把复杂概念翻译为家庭决策问题。\n6. 不照搬参考内容的独特句子、结构、比喻和案例组合。\n7. 根据平台调整信息密度、开头速度和篇幅。\n\n【输出】\n优先输出完整、可直接使用的正文；除非明确要求，不额外输出研究过程、策略解释或机械互动话术。`;
const growth=`【职责】\n你负责判断内容为什么写、写给谁，以及如何通过内容获得关注、建立信任并自然推动业务关系；不覆盖正文语言和结构。\n\n【增长方法】\n1. 判断目标用户处于陌生、关注、信任还是决策阶段，单篇只选择一个主要任务。\n2. 陌生阶段建立相关性；关注阶段提供新判断；信任阶段展示方法和边界；决策阶段帮助完成下一步核对或咨询。\n3. 区分流量、信任和转化内容，不把每篇都写成成交内容。\n4. 通过具体问题、真实场景、清晰机制和可执行判断建立信任，不依赖焦虑和效果承诺。\n5. 只有正文已提供充分价值且阶段合适时才设计自然承接。\n6. 没有承接条件时允许不转化。\n7. 复盘观察目标用户互动、有效咨询和行动完成度，不只看曝光。\n\n【输出】\n输出目标用户、认知阶段、主要任务、信任机制和下一步行动，作为内容创作的策略输入。`;
const card={title:"专业内容基础创作教练",summary:"帮助财经、财富、保险与其他专业服务者识别题目领域、明确IP方向，并以克制可信的方式建立关注、信任与业务承接。",scenarios:["领域判断","IP定位","专业内容创作","获客增长"],styleTags:["尊重原题","专业可信","自然表达","克制增长"],bestFor:"尚未建立专属教练，或希望使用平台多领域通用方法的专业服务者",domainScope:["general","finance","wealth","insurance","hybrid"],primaryDomain:"general",allowsCrossDomainMigration:true,migrationRules:["用户明确指定领域时不得迁移","纯财经内容不得强行保险化","交叉题先说明真实连接再迁移"]};
const client=await pool.connect();try{await client.query("begin");const owner=await client.query(`select id from users where role='admin' and status='active' order by created_at limit 1`);if(!owner.rows[0])throw new Error("没有可用管理员");const userId=owner.rows[0].id;const coach=await client.query(`insert into creative_coaches(user_id,name,creator_name,coach_scope,status,latest_version,identity_card,is_system) values($1,'小谷教练','小谷','platform','active',0,$2::jsonb,true) on conflict (is_system) where is_system=true do update set name='小谷教练',status='active',identity_card=excluded.identity_card,updated_at=now() returning id`,[userId,JSON.stringify(card)]);const coachId=coach.rows[0].id;const existing=await client.query(`select id from creative_coach_versions where coach_id=$1 and version=1`,[coachId]);let versionId=existing.rows[0]?.id;if(versionId)await client.query(`update creative_coach_versions set status='active',ip_positioning_prompt=$2,content_creation_prompt=$3,growth_prompt=$4,change_summary='系统内置基础创作方法 V1' where id=$1`,[versionId,ip,content,growth]);else{const version=await client.query(`insert into creative_coach_versions(coach_id,user_id,version,status,ip_positioning_prompt,content_creation_prompt,growth_prompt,sample_count,change_summary) values($1,$2,1,'active',$3,$4,$5,0,'系统内置基础创作方法 V1') returning id`,[coachId,userId,ip,content,growth]);versionId=version.rows[0].id;}await client.query(`update creative_coach_versions set status='superseded' where coach_id=$1 and id<>$2 and status in('active','restored')`,[coachId,versionId]);await client.query(`update creative_coaches set latest_version=1,status='active',updated_at=now() where id=$1`,[coachId]);await client.query("commit");console.log(JSON.stringify({name:"小谷教练",coachId,versionId,version:1,status:"active"}));}catch(error){await client.query("rollback");throw error;}finally{client.release();await pool.end();}
