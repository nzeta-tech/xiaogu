# 专享权限、口播资产、口播生成：上线准备核查

核查时间：2026-09-18（北京时间）。结论：**尚不具备整组功能直接上线条件**。本次没有切换线上代码、修改线上数据库或启动付费生成。

后续安全清理已完成：69 / 48 / 167 可用空间分别约 9.3 / 11 / 11 GB；旧混合声音资产包已删除，新照片包保留且再次逐台校验通过。详见 [清理记录](PRODUCTION_CLEANUP_20260918.md)。下文低磁盘数值为清理前快照；媒体服务仍未部署。

## 再次实测：2026-09-18（北京时间）

**仍未达到整组上线条件。** 本轮只读核查生产，并同步独立公共照片包，没有执行迁移、切换 current、重启服务或开启口播生产入口。

- 三台 Web 仍为 `40096d527c9f`，ALB 三目标 healthy，公网 readiness `ready: true`。工作区仍有未提交/未跟踪功能变更，没有干净候选发布 SHA。
- 线上迁移仍仅到 080，081–086 均缺失；`verified_payment_receipts`、`exclusive_app_access_overrides`、`spoken_photo_assets` 不存在。声音和媒体表各 0 行。口播应用 `points_cost=12`，尚未执行 50 积分定价。实付状态且正金额订单聚合为 0，不需本轮历史支付回填。
- 线上容器未配置 MEDIA_NODE_URL/TOKEN/ID；三台 Web `/app` 均只读，没有媒体数据盘挂载，Nginx 均为 50m。三节点根盘可用空间依次约 1.2 / 2.5 / 3.1 GB。
- Agent 最新心跳版本 `30bcf34c2e4d`，protocol 1，仅 source.inspect / ppt.generate 能力，无 digital-human.video.produce。生产 Agent 容器有 FFmpeg/ffprobe，没有 HeyGen/Codex CLI。Dockerfile 未打包 spoken-video-production、spoken-video-poll、spoken-video-quality 依赖模块。
- 录音克隆仍在 Web 执行 FFmpeg/HeyGen CLI，必须迁至有工具及账号的 Agent，并验证创建、查询、失败与重试。当前字幕/标题使用 PingFang SC；在实际 worker 验证中文字体及渲染，不能沿用旧报告的 Songti 文件路径判断。
- 本轮本地测试：充值规则 4 项通过，数据库集成 1 项跳过（原 paid_access_test 隔离库不存在）；媒体节点 9 项通过；供应商下线及口播确定性测试 13 项通过。此前隔离支付/人工授权证据仍保留，但不能替代候选版本数据库和 HTTP 回归。没有完成真实端到端口播生成与扣费验收。
- 本地开发库再次只读核实：4 张个人照片、1 个 ready HeyGen 声音。目标线上账号映射待用户提供；私人媒体、资产关系和 HeyGen 账号可用性需一起核对，当前未迁移，不作为公共模板发布。

### 本轮资产同步完成

新包仅含 171 张公共照片和 1 个目录文件，共 **172 个文件 / 317,629,475 字节**，声音条目为 0。

- 三台服务器相同目录：`/home/ubuntu/insurance-content-agent/asset-bundles/photos-7ccf4d13e87672ee/`。
- manifest SHA-256：`7ccf4d13e87672ee8b483179713a7f458e9fbbb466c34888699c869d2e056e6b`。
- 16.176.34.69、16.176.18.48、3.106.138.167：每台 `sha256sum -c manifest.sha256` 172/172 通过，manifest 哈希一致。本地以当前人物库代码切换到新包目录读取，171 张全部可加载。
- 通过 rsync link-dest 复用旧包中未变化的照片字节，没有改动旧包或 active runtime。此前 91 个模板声音及试听文件未删除，但已退出发布范围。
- **同步完成不等于线上生效**：新包必须合入不可变 Web runtime 的 `research/provider-scene-catalog/2026-08-27` 路径，再完成三节点接口验收；当前没有切换线上页面。

### 发布顺序与结论

1. 充值用户功能可独立推进候选验收：应用 084/086；如整组发布，按序应用全部 081–086。重跑支付验签/入账幂等、管理员不能凭身份解锁、赠送积分不解锁、手工开通/禁止/到期及权限后台 HTTP 回归。
2. 补齐录音任务 Agent 化、Agent 模块打包/能力、HeyGen/Codex 身份、中文字体。
3. 落实持久磁盘及固定媒体服务，三 Web 配置一致；应用 500 MiB 上传路径配置，验收真实网关 >50 MiB 上传和跨节点读取。
4. 接入公共照片新包；确认私有资产归属并迁移。录音声音需对应的 HeyGen 账户能查询和使用，复制数据库行不能证明可用。
5. 形成干净候选 SHA，完成权限、资产生命周期、照片+录音声音→成片→播放下载→成功扣费一次的候选端到端回归，再按协调发布流程逐节点切换并开放入口。

本轮回归目录决定：**feature deferred**。已复用充值/媒体/下线确定性测试，但缺少候选真实口播生命周期证据，不执行生产 promotion。以下保留此前详细核查与历史同步证据；当前事实以本节为准。

## 后续范围确认：禅境能力下线

2026-09-18 最新确认覆盖此前保留方案：**整体移除模板声音和禅境在线服务依赖**。新版“我的声音库”只保留录音克隆；声音列表过滤历史禅境记录，口播创建只接受 HeyGen 声音，Agent 拒绝历史禅境声音任务。模板导入、旧声音收藏/克隆/刷新、试听合成和 Agent TTS 接口均保留鉴权并返回 410。供应商层已删除禅境网络调用代码及凭据读取。

已有照片、媒体和历史作品不删除，历史 provider 字段保留用于兼容读取。已下载的公共照片继续本地读取，其目录名称中保留 chanjing 不代表运行时依赖。91 个公共声音及试听文件不再属于发布资产，也无需配置 CHANJING_APP_ID / CHANJING_SECRET_KEY。资产导出脚本仅导出照片，旧 --voices 参数明确拒绝；以前同步的声音包未从服务器删除，但不接入运行环境。

本次仅修改候选代码，未发布线上。录音克隆的 Web/Agent 执行环境、存储部署和数据库迁移仍是上线前置条件。

验证：`npm run test:provider-retirement` 11 项通过，包括旧凭据不能恢复调用、模板导入/TTS 返回 410、鉴权、历史声音隐藏、录音声音保留和 Agent 旧任务拒绝。应用 src 独立类型检查通过；核心改动定向 ESLint 通过。扩大到资产页和口播页的 lint 仍有原有 React effect 报错及图片警告，不记为全量通过；未做付费生成或生产发布。

## 固定媒体节点候选实现

2026-09-18 用户已选定 `16.176.34.69`（内网 `172.31.20.28`）作为媒体存储节点，部署环境示例已更新。节点选择已完成；持久磁盘、媒体服务部署、三节点配置及网关验收仍待完成，不能将“已指定”记为“已启用”。

已实现无需 S3/NFS 的独立媒体节点、持久磁盘挂载配置、Web 流式上传/读取、用户归属检查和 Range 支持。Agent 回传上限统一为 500 MiB，提供受限路径 Nginx 配置。详见 [部署说明](MEDIA_NODE_DEPLOYMENT.md)。以下表格中的存储和网关阻塞现在已有候选实现，但**生产仍未部署媒体服务、未挂载数据盘、未修改 50m 限额**，也未开放口播生产开关。

## 已核实的线上基线

- 三台 Web（16.176.34.69 / 16.176.18.48 / 3.106.138.167）均指向 `40096d527c9f`，ALB 三个目标均 healthy；本地 HEAD 同版本。
- 功能变更仍在工作区，包括未跟踪的 081–086 SQL、新 API、前端和 Agent 脚本；尚无包含本功能的干净发布 SHA。
- 从线上运行中的 app 容器读取数据库：已记录全部仓库现有 001–080 迁移，081–086 缺失。口播应用目前 12 积分，新版要求 50 积分。
- 线上 Agent 版本 `30bcf34c2e4d`；当前心跳仅提供 `source.inspect` 和 `ppt.generate`，没有 `digital-human.video.produce`。
- 线上口播照片表不存在；声音资产、媒体资产均为 0。真实充值凭证表及人工授权表尚不存在。
- 本地实际开发库已到 085，086 尚未执行（此前 086 在隔离回归库验证通过）。开发库有 4 张个人口播照片、1 个 ready 的 HeyGen 声音；这些是账号私有数据，未混入公共资产包。

## 依赖与阻塞

| 功能 | 状态 | 上线前必须完成 |
| --- | --- | --- |
| 真实充值门槛、管理员人工授权 | 实现与隔离回归完成，线上未启用 | 执行 084、086；上线支付回调、统一准入、后台界面；验证人工开通/禁止/到期回退/并发修改/积分不足 |
| 口播任务与资产表 | 线上缺失 | 按序执行 081（任务类型及媒体类型）、082（照片表）、083（模板来源字段） |
| 每次 50 积分与成功扣费一次 | 线上仍 12 积分 | 084 完成触发器与幂等索引、085 定价；验收成功仅扣一次、失败不扣、重复进度不重复扣 |
| 公共照片模板 | 已整理独立发布资产包 | 当前代码读取 `research/provider-scene-catalog/2026-08-27`；必须把资产纳入每个不可变 runtime，或显式配置只读共享资产根并挂载；不能只上传后认为应用已能读取 |
| 公共声音库 | 已整体下线 | 不发布目录/试听文件，不配置禅境凭据；历史声音不能用于新任务 |
| 录音克隆声音 | 当前 Web/Agent 职责不匹配 | `/api/spoken-voices` 在 Web 内调用 FFmpeg/HeyGen CLI；生产 `node:22-alpine` Web 无此工具。应迁至 Agent 异步任务，同时迁移创建、轮询、失败恢复；不能靠仅更新 Web 发布解决 |
| 视频生成 Agent | 线上能力缺失 | 发布新 Agent 编排器和 `spoken-video-production.mjs`、`spoken-video-poll.mjs`；当前 Dockerfile 仅显式复制 local-agent 主脚本，未打包这两个新模块；生产启动能力列表也需加入新任务类型 |
| Agent 工具与身份 | 尚未端到端验证 | 选用具备已登录 HeyGen CLI、Codex、FFmpeg/ffprobe 的宿主机 worker，或提供等价候选运行环境；现有生产容器有 FFmpeg/ffprobe，但没有 HeyGen/Codex CLI。生产宿主机 PPT worker 固定仅领取 PPT，不能自动处理口播任务 |
| 中文字体 | 实际 worker 尚未验收 | 当前字幕/标题使用 PingFang SC；Linux worker 需提供可用中文字体，并验证字幕和图文渲染 |
| 媒体持久存储与三节点读取 | 候选实现完成，部署仍阻塞 | 生产 Web `/app` 只读，未挂载媒体目录，S3 未配置。候选代码已接入固定媒体节点；需要挂载持久盘、部署媒体服务、三节点配置一致并验收跨节点读取 |
| 成片上传网关 | 候选配置完成，线上未应用 | 三节点 Nginx 当前均 `client_max_body_size 50m`；候选 Web、媒体服务和 Nginx 上传配置统一为 500 MiB。必须应用配置并验收真实网关大小边界、超时；否则大成片仍会被 413 拒绝 |
| 照片上传、模板导入、换脸 | 需候选回归 | 在真实部署拓扑验证上传→保存→刷新→跨节点读取、所有权隔离；换脸依赖图片模型，需单独验证凭据/结果持久化/失败路径 |
| 成片业务回归 | **未完成** | 候选环境完成照片+声音→生成→远端任务 ID 持久化→母版/成片/封面上传→播放下载→积分核对，并验证断线恢复、失败、重试不会重复远端提交 |

线上历史订单只读汇总未发现 `paid/completed` 且金额大于 0 的记录，因此本次核查没有待回填的存量实付订单。正式上线前应重新查询；不要用赠送积分或管理员身份推导实付资格。

## 公共资产包（此前同步记录，声音部分现已退出发布范围）

通过 `scripts/prepare-spoken-public-assets.mjs` 生成，只包含平台公共素材，不包含用户记录、凭据或远端签名 URL。

- 171 张当前照片库实际展示的人物照片；保留当前过滤逻辑及原图字节。
- 91 个禅境公共声音条目及 91 份试听音频。
- 264 个业务文件，355,101,868 字节（约 339 MiB）。
- `manifest.json` SHA-256：`3e2d775e711556d5dd16544c4208563176293bc4d952cdab5bf18af19a89e42d`。
- 目标目录：三台 Web 的 `/home/ubuntu/insurance-content-agent/asset-bundles/spoken-3e2d775e711556d5/`。
- **同步已完成**：16.176.34.69、16.176.18.48、3.106.138.167 三节点均执行 `sha256sum -c manifest.sha256` 成功（每台 264/264）；各节点 `manifest.json` 哈希与上文一致。
- 该目录是**待发布资产**，不在当前 `/app` 挂载内，未改变当前线上页面；正式发布仅接入公共照片；声音目录和试听文件不再接入。
- 旧版“高表现力/成片视频模板”另外读取 `research/chanjing-ai-templates` 和 `research/chanjing-video-templates`（约 2.5 GB），不属于当前口播照片入口所需文件，未混入这次照片/声音包。如继续对用户开放旧入口，需单独纳入资产发布范围，不能依赖空列表降级。
- 个人照片与克隆声音需要明确本地账号→线上账号映射，并同时迁移媒体字节、媒体行、资产行和供应商账号可用性，不能只复制表或把个人声音变成公共声音。

## 验证证据与发布顺序

- 上一轮专享权限/计费：20 项隔离测试通过；HTTP 鉴权、人工授权、禁止、恢复自动、积分不足及后台 UI 已验证。
- 本轮口播确定性测试：`node --experimental-strip-types --test scripts/spoken-video-production.test.mjs scripts/spoken-video-poll.test.mjs`：4 项通过，覆盖轮询错误/重试上限/字幕拆行。**不等价于完整成片验收**。
- 本轮三节点 SHA、ALB 状态、线上迁移和资产数量来自实际只读查询。
- 公共资产包在独立目录以当前照片库读取代码重新加载：171 张可读；91 份声音试听均存在；264 个文件本地 SHA-256 全部一致。
- 回归目录判断：专享准入已有部分覆盖；口播资产生命周期与整条 Agent 视频生产尚缺可执行的候选端到端发布回归，发布前应注册到 xiaogu-deploy 对应回归目录。

建议顺序：先修复录音任务归属、Agent 打包/能力、共享媒体存储和资产目录接入；完成私有数据迁移范围确认；形成干净候选 SHA；候选环境应用 081–086 并接入同一资产包；跑权限、资产和真实成片回归；再执行正式协调发布，逐节点切换，核验全部节点能读取同一份照片/声音/成果；最后开放入口。迁移涉及旧新任务兼容，不能只发 Web。

目前不执行正式上线的原因是已发现的实际依赖缺口。发布技能 [xiaogu-deploy](/Users/a2251/.codex/skills/xiaogu-deploy/SKILL.md) 还要求：`A missing required fixture or runner means the relevant capability is not validated. Stop promotion or explicitly remove that capability from the release; do not label the release fully verified.` 本次报告据此将端到端验收标记为未完成，没有把确定性测试通过等同于可发布。
