# 视频优化回滚

用户要求撤回上次视频优化。以线上 91ea1f9e18ca 为父提交，视频生成代码恢复到 674c793。此次仅发布宿主机制作 Agent，Web 91ea1f9e18ca 和协议 1 保持兼容。

撤回：新增导演/证据规划、12 秒强制小窗预检、整卡缩小、SVG 画布预检、硬件编码探测、单次合成、批量/并行抽帧、增量质检、跨任务渲染和素材缓存、扩展阶段计时、同批新增动作提示。原基线已有的任务内镜头缓存和研究断点机制仍属于原流程。

保留：1080P、无栏目署名、三轮后带真实问题提示交付、技术错误仍失败、后续 CDN 413 分片上传修复及账号隔离、不重新生成口播母片。口播文案 5+1 和流式修复不属于本次视频回滚。

与 674c793 的 production 模块差异仅为分片上传和三轮交付兼容。旧的通用知识卡模板缺陷在该基线中仍存在；回滚不等于证明全部视频画质合格。

回归：恢复旧生成路径的测试，并按仍然保留的三轮交付规则更新断言；新增真实 13 秒长全屏知识卡合成像素检查，防止在最终合成中被强制加回小窗。更新技能回归目录，明确删除优化专属模块/测试属于用户要求撤回。
