# V2 混剪发布记录

状态：Web 三节点与制作 Agent 已上线 `c762c201270f`，发布验收通过；用户 V2 已提交，成片尚未验收。

## 版本与范围

- 干净发布分支：`codex/workbuddy-video-release-20260920`。
- 候选：`c762c201270f`；Web 基线 `b935c109e334`，制作 Agent 基线 `a128b39d1baf`。
- 修复字幕与 PIP 坐标尺度、安全区、排版定向修复及片段缓存。
- 基础版知识卡也使用全屏；智能版数据/机制段全屏，保留口播主讲段。
- 支持从已有本人母片的失败任务创建独立 V2；原失败状态不修改，V2 任务仅包含 `mode=recut`，不包含口播生成参数。
- 无历史分镜时从锁定原文重建画面计划；母片必须从服务端归档读取。

## 发布前验证

- 61 项视频/声音/计费门禁回归通过，包含真实 FFmpeg 像素检查、缓存复用、18 段金融文案单段修复，以及缺失分镜恢复的母片读取边界。
- 已验证 Web 前向继承、无删除；既有 200 个页面/API 路由保留，96 个迁移内容不变，37 个 package scripts 不变；线上 96 个迁移已应用。
- 发布回归目录：`existing coverage` / `merged`，新增安全区、缓存和失败母片继续混剪负例已加入 spoken lifecycle 与 quality delivery。
- Web/Host 使用不可变发布包，不覆盖当前运行目录，不重启不变的 Docker 抓取服务。

## 进行中

生产构建、Linux Alpine/x64 原生依赖、候选版本 HTTP、滚动发布、公网冒烟通过。
候选版本 HTTP 已覆盖失败母片恢复、缺少母片拒绝、原状态不变、所有权、幂等、并发与版本选择；质检交付 23 项通过。
Studio 持久化回归：微信 8、小红书 11、流量封面 9 项通过。
三节点 staged manifest 一致，ALB 恢复 healthy，无应用回滚；Web 发布阶段耗时 399 秒（不含先行构建和候选验收）。
Web artifact 139,165,696 字节；manifest SHA-256 `c85037f7e99f9a6b997a49b0316de069935139790f0eb2c6bb5e9508beb487a4`。
制作 Agent manifest SHA-256 `3d40f0599ddf5bc4c63502979216dca6b879272763e6ec19f70b33797e76a5e5`；协议 1，三项制作依赖健康。
线上质检交付回归 23 项通过；公网 readiness=true，所有必需依赖健康。既存可选 OpenMeter 遥测异常仍在，不影响已验证的积分事务，不报告为已修复。
正式 Web 报告：`~/.xiaogu-deploy/reports/c762c201270f-web.json`。
用户 V2 必须单独验证原母片 SHA 未变、任务为 recut、无新增 provider job、修订积分费用为零。

## 线上结果

- 开关已开启，新版本制作心跳正常；真实来源回归完成，10 个 SSE delta、1417 字转录，流式结果与持久化一致，无敏感媒体字段。
- 激活后再次检查三个节点 current 均为 c762c201270f，完整 manifest 校验通过且一致，容器 running。
- 用户 V2 已由正式版本 API 幂等提交，版本号 2、修订积分费用 0；请求标识与母片校验值保存在本机 `outputs/guyu-photo-20260920/v2-recut.json`，未保存会话凭据。
- 原任务仍为失败，不将其伪装成已完成。V2 需要重建未保存的分镜与画面，而非重新生成母片。
